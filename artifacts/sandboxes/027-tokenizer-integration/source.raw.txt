<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>ELM369 \u00b7 Tokenizer Integration Artifact</title>
<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&family=Orbitron:wght@400;700;900&family=Exo+2:wght@300;400;600&display=swap" rel="stylesheet"/>
<style>
  :root {
    --elm-cyan: #00f5d4;
    --elm-dark: #020c10;
    --elm-panel: #041820;
    --elm-border: #0a3040;
    --elm-accent: #00bfa5;
    --elm-warn: #ff6b35;
    --elm-gold: #ffd166;
    --elm-green: #06d6a0;
    --elm-red: #ef233c;
    --elm-dim: #1a4a5a;
    --font-mono: 'Share Tech Mono', monospace;
    --font-display: 'Orbitron', sans-serif;
    --font-body: 'Exo 2', sans-serif;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    background: var(--elm-dark);
    color: #c8e6ea;
    font-family: var(--font-body);
    min-height: 100vh;
    overflow-x: hidden;
  }
  /* Animated grid background */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(0,245,212,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,245,212,0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  .container { max-width: 1100px; margin: 0 auto; padding: 24px 16px; position: relative; z-index: 1; }

  /* HEADER */
  .header {
    border: 1px solid var(--elm-cyan);
    background: linear-gradient(135deg, #041820 60%, #071e2a);
    padding: 24px 28px;
    margin-bottom: 20px;
    position: relative;
    overflow: hidden;
    animation: fadeSlide 0.6s ease both;
  }
  .header::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--elm-cyan), transparent);
    animation: scanline 3s linear infinite;
  }
  @keyframes scanline { 0%{opacity:0.3} 50%{opacity:1} 100%{opacity:0.3} }
  @keyframes fadeSlide { from{opacity:0;transform:translateY(-12px)} to{opacity:1;transform:none} }

  .header-top { display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 12px; }
  .project-id { font-family: var(--font-mono); font-size: 10px; color: var(--elm-accent); letter-spacing: 1.5px; }
  .header h1 { font-family: var(--font-display); font-size: clamp(16px, 3vw, 22px); color: var(--elm-cyan); letter-spacing: 2px; margin: 10px 0 4px; }
  .header .sub { font-family: var(--font-mono); font-size: 11px; color: #5a9aaa; }
  .stamp-badge {
    background: var(--elm-cyan);
    color: var(--elm-dark);
    font-family: var(--font-display);
    font-size: 9px;
    font-weight: 700;
    padding: 4px 10px;
    letter-spacing: 2px;
    align-self: flex-start;
  }

  /* META ROW */
  .meta-row {
    display: flex; flex-wrap: wrap; gap: 8px;
    font-family: var(--font-mono); font-size: 10px; color: #3a7a8a;
    margin-bottom: 20px;
    animation: fadeSlide 0.7s ease both;
  }
  .meta-row span { border: 1px solid var(--elm-dim); padding: 3px 8px; }
  .meta-row .live { color: var(--elm-green); border-color: var(--elm-green); }

  /* SECTION CARD */
  .card {
    border: 1px solid var(--elm-border);
    background: var(--elm-panel);
    margin-bottom: 16px;
    animation: fadeSlide 0.7s ease both;
  }
  .card-header {
    background: linear-gradient(90deg, #071e2a, #041015);
    border-bottom: 1px solid var(--elm-border);
    padding: 10px 16px;
    display: flex; align-items: center; gap: 10px;
  }
  .card-header .icon { font-size: 14px; }
  .card-header h2 { font-family: var(--font-display); font-size: 11px; color: var(--elm-cyan); letter-spacing: 2px; }
  .card-header .tag { margin-left: auto; font-family: var(--font-mono); font-size: 9px; color: var(--elm-gold); border: 1px solid var(--elm-gold); padding: 2px 6px; }
  .card-body { padding: 16px; }

  /* TABLES */
  table { width: 100%; border-collapse: collapse; font-size: 12px; }
  th {
    font-family: var(--font-mono); font-size: 10px; letter-spacing: 1px;
    color: var(--elm-cyan); text-align: left;
    border-bottom: 1px solid var(--elm-dim); padding: 6px 10px;
    background: rgba(0,245,212,0.04);
  }
  td { padding: 8px 10px; border-bottom: 1px solid rgba(10,48,64,0.6); vertical-align: top; line-height: 1.5; }
  tr:last-child td { border-bottom: none; }
  tr:hover td { background: rgba(0,245,212,0.03); }

  .tier { font-family: var(--font-mono); font-size: 10px; font-weight: 700; padding: 2px 7px; border-radius: 0; display: inline-block; }
  .tier-platinum { background: #e2e8f0; color: #020c10; }
  .tier-gold { background: var(--elm-gold); color: #020c10; }
  .tier-silver { background: #94a3b8; color: #020c10; }
  .tier-bronze { background: #c2855a; color: #020c10; }
  .tier-flagged { background: var(--elm-red); color: #fff; }
  .tier-verified { background: var(--elm-green); color: #020c10; }

  .role-tag { font-family: var(--font-mono); font-size: 10px; color: var(--elm-accent); border: 1px solid var(--elm-dim); padding: 1px 6px; display: inline-block; }

  /* FRAMEWORK GRID */
  .fw-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 10px; }
  .fw-card {
    border: 1px solid var(--elm-dim);
    padding: 12px;
    background: rgba(0,245,212,0.02);
    transition: border-color 0.2s;
  }
  .fw-card:hover { border-color: var(--elm-cyan); }
  .fw-name { font-family: var(--font-display); font-size: 13px; color: var(--elm-cyan); margin-bottom: 6px; }
  .fw-expand { font-size: 11px; color: #5a9aaa; line-height: 1.6; margin-bottom: 6px; }
  .fw-score { font-family: var(--font-mono); font-size: 10px; }

  /* VAULT LOG */
  .log-entry {
    font-family: var(--font-mono); font-size: 11px;
    border-left: 2px solid var(--elm-cyan);
    padding: 8px 12px;
    margin-bottom: 8px;
    background: rgba(0,245,212,0.02);
  }
  .log-entry .log-time { color: var(--elm-gold); font-size: 10px; }
  .log-entry .log-level { font-size: 10px; margin-left: 8px; }
  .log-entry .log-msg { color: #c8e6ea; margin-top: 3px; line-height: 1.6; }
  .log-ok { border-color: var(--elm-green); }
  .log-warn { border-color: var(--elm-warn); }
  .log-info { border-color: var(--elm-cyan); }

  /* OMNI NODE */
  .node-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 10px; }
  .node {
    border: 1px solid var(--elm-dim);
    padding: 12px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  .node::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 2px;
    background: var(--elm-cyan);
    transform: scaleX(0);
    transition: transform 0.3s;
  }
  .node:hover::after { transform: scaleX(1); }
  .node .node-label { font-family: var(--font-mono); font-size: 9px; color: var(--elm-accent); letter-spacing: 1.5px; margin-bottom: 4px; }
  .node .node-name { font-family: var(--font-display); font-size: 11px; color: var(--elm-cyan); }
  .node .node-desc { font-size: 10px; color: #4a8090; margin-top: 4px; }

  /* DIRECTIVE BOX */
  .directive {
    border: 1px solid var(--elm-warn);
    background: rgba(255,107,53,0.05);
    padding: 14px 16px;
    font-family: var(--font-mono);
    font-size: 11px;
    line-height: 1.8;
    margin-bottom: 16px;
    color: #e0a898;
  }
  .directive .d-label { color: var(--elm-warn); font-size: 10px; letter-spacing: 2px; margin-bottom: 6px; }

  /* FOOTER */
  .footer {
    border-top: 1px solid var(--elm-dim);
    padding: 16px 0 8px;
    font-family: var(--font-mono);
    font-size: 10px;
    color: #2a5a6a;
    display: flex; flex-wrap: wrap; justify-content: space-between; gap: 8px;
  }
  .footer .elm-sig { color: var(--elm-cyan); }

  .pulse { animation: pulse 2s infinite; }
  @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:0.4} }
</style>
</head>
<body>
<div class="container">

  <!-- HEADER -->
  <div class="header">
    <div class="header-top">
      <div>
        <div class="project-id">PROJECT ELM369 \u00b7 JMR0824197846902 \u00b7 ID: 1550e4d5-9ee3-49cd-8af8-7c9d630f84ad</div>
        <h1>TOKENIZER INTEGRATION ARTIFACT</h1>
        <div class="sub">SOURCE: ChatGPT Cheat Sheet (901.png) \u00b7 CLASSIFICATION: AI TEAM DIRECTIVE \u00b7 PANDORA VAULT INPUT</div>
      </div>
      <div class="stamp-badge pulse">\u25cf ACTIVE</div>
    </div>
  </div>

  <!-- META -->
  <div class="meta-row">
    <span class="live">\u25cf SECURITY LOG 1 WRITE</span>
    <span class="live">\u25cf SECURITY LOG 2 WRITE</span>
    <span class="live">\u25cf PANDORA VAULT WRITE</span>
    <span>TIMESTAMP: <span id="ts"></span></span>
    <span>LOCATION: KOKOMO, IN \u00b7 USA</span>
    <span>ARTIFACT REV: 1.0.0</span>
    <span>AI TEAM NOTIFY: ACTIVE</span>
  </div>

  <!-- DIRECTIVE -->
  <div class="directive">
    <div class="d-label">\u26a0 ELM369 INTEGRATION DIRECTIVE</div>
    This artifact maps the ChatGPT Cheat Sheet (ref: 901.png) into Project ELM369 infrastructure layers. All prompt frameworks are scored and integrated into Tokenizer compliance tiers. All AI tools are assigned to AI Team roles. All GPT-type agents are registered as Tokenizer benchmark candidates. OMNINET node assignments confirmed. No promotional content (ref: "$100 Free Guide" CTA) has been imported \u2014 filtered by Tokenizer FLAGGED tier logic. Pandora Vault entry logged. Security Log 1 &amp; 2 updated. AI Team notified.
  </div>

  <!-- SECTION 1: TOOL \u2192 AI TEAM ROLE MAP -->
  <div class="card">
    <div class="card-header">
      <span class="icon">\ud83e\udd16</span>
      <h2>SECTION 1 \u00b7 AI TOOL \u2192 ELM369 AI TEAM ROLE