# Pandora Vault / Security Logs

Local Security Log 1, Security Log 2, and Pandora Vault feeds (from issues #11 / #29).

```bash
python3 -m tools.pandora_vault sync --message "vault sync check"
python3 -m tools.pandora_vault tail --channel pandora
```
