# elm_obfuscate

Toy classical obfuscation for local demos. **Not encryption.** Do not protect secrets with this.

```bash
python3 -m tools.elm_obfuscate obfuscate "hello"
python3 -m tools.elm_obfuscate deobfuscate "…"
```
