# Offline engine scaffold

Issue #14. Snapshots key docs/backlogs for local use. Does **not** control satellites, radios, or hotspots.

```bash
python3 -m tools.elm_offline snapshot
python3 -m tools.elm_offline status
```
