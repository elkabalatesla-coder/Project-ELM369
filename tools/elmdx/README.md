# ELMDX — Android diagnostics scaffold

Issue #26. Scores a **provided** inventory JSON. Does not root devices or run ADB.

```bash
python3 -m tools.elmdx diagnose
python3 -m tools.elmdx diagnose --inventory path/to/inventory.json
```
