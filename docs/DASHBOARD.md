# ELM369 Mission Control Dashboard

Status: OPERATIONAL  
Schema: `elm369.mission_control_dashboard.v1`

## Authority

The runtime dashboard is generated by `tools/elm_dashboard`. Its source-of-truth inputs are the live ELM369 runtime tools: progress, path verification, vault status, artifact verification, devtools inventory, roster lanes, and case queue.

HTML files under `artifacts/` are presentation artifacts and must not be treated as an independent status database.

## Operator commands

```bash
python3 -m tools.elm_dashboard show
python3 -m tools.elm_dashboard show --no-cases
python3 -m tools.elm_dashboard roster
python3 -m tools.elm_dashboard cases
```

## Dashboard tabs

COMMAND · TOOLS · SECURITY · VAULT · PROGRESS · ROSTER · CASES

## Current project state

The formal registry snapshot records 26 tools, with 25 DONE and 1 SCAFFOLD and an average completion of 88.3%. The documented remaining scaffold is AUDIO-TX under `tools/elm_translator`.

## Synchronization rule

When presentation artifacts and runtime output disagree, runtime dashboard output wins. Update documentation after material runtime changes so status does not drift.

Updated: 2026-09-25
