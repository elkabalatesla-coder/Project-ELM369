[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project-elm369"
version = "0.0.1"
description = "Timezone compatibility helpers for Project-ELM369"
requires-python = ">=3.8"
dependencies = [
  "backports.zoneinfo>=0.2 ; python_version < '3.9'",
]
